import { useState } from "react";
import type { NavItem } from "../../types/index.js"; import "./Sidebar.css";
const NAV_ITEMS: NavItem[] = [ { id: "analyze", label: "Analyze Field Impact", icon: "⚡", active: true }, { id: "table-automation", label: "Find Table Automation", icon: "📋", active: false, comingSoon: true }, { id: "explain-br", label: "Explain Business Rule", icon: "🔍", active: false, comingSoon: true }, { id: "update-set", label: "Analyze Update Set", icon: "📦", active: false, comingSoon: true }, { id: "dependency-graph", label: "Dependency Graph", icon: "🕸️", active: false, comingSoon: true } ];
export function Sidebar() { const [activeId, setActiveId] = useState("analyze"); return (<aside className="sidebar"><div className="sidebar-brand"><span className="sidebar-logo">⚡</span><div className="sidebar-brand-text"><span className="sidebar-title">Impact Analyzer</span><span className="sidebar-subtitle">ServiceNow</span></div></div><nav className="sidebar-nav"><div className="sidebar-section-label">Tools</div>{NAV_ITEMS.map((item) => (<button key={item.id} className={`sidebar-item ${activeId === item.id ? "active" : ""} ${item.comingSoon ? "disabled" : ""}`} onClick={() => !item.comingSoon && setActiveId(item.id)} disabled={item.comingSoon}><span className="sidebar-item-icon">{item.icon}</span><span className="sidebar-item-label">{item.label}</span>{item.comingSoon && <span className="sidebar-badge">Soon</span>}</button>))}</nav><div className="sidebar-footer"><span className="text-secondary text-sm">v2.1.0</span></div></aside>); }
