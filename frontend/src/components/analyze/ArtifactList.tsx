import type { ImpactResultEntry, Classification } from "../../types/index.js"; import { Collapsible } from "../common/Collapsible.js"; import "./ArtifactList.css";
const CLS_COLORS: Record<Classification, string> = { TRIGGER: "var(--trigger-color)", READ: "var(--read-color)", WRITE: "var(--write-color)", REFERENCE: "var(--reference-color)" };
const CLS_EMOJI: Record<Classification, string> = { TRIGGER: "⚡", READ: "📖", WRITE: "✏️", REFERENCE: "🔗" };
export function ArtifactList({ title, icon, entries }: { title: string; icon: string; entries: ImpactResultEntry[] }) { if (entries.length === 0) return null; return (<Collapsible title={`${icon}  ${title}`} badge={entries.length} defaultOpen={true}><div className="artifact-entries">{entries.map((e) => <ArtifactEntry key={e.sys_id} entry={e} />)}</div></Collapsible>); }
function ArtifactEntry({ entry }: { entry: ImpactResultEntry }) { return (<Collapsible title={entry.name || "(unnamed)"} variant="subtle"><div className="artifact-detail"><div className="artifact-meta-row"><span className="artifact-meta"><span className="text-secondary">sys_id:</span> <code>{entry.sys_id}</code></span><span className="artifact-meta"><span className="text-secondary">Active:</span> <span className={entry.active ? "status-active" : "status-inactive"}>{entry.active ? "Yes" : "No"}</span></span></div>{entry.description && <p className="artifact-description">{entry.description}</p>}<div className="artifact-classifications"><span className="artifact-section-label">Classifications</span>{entry.classifications.map((c, i) => (<div key={i} className="classification-tag" style={{ borderLeftColor: CLS_COLORS[c.classification] }}><span>{CLS_EMOJI[c.classification]}</span><span className="classification-tag-type" style={{ color: CLS_COLORS[c.classification] }}>{c.classification}</span>{c.lineNumber && <span className="classification-tag-line">line {c.lineNumber}</span>}<code className="classification-tag-evidence">{c.evidence}</code></div>))}</div>{Object.keys(entry.extra).length > 0 && <div className="artifact-extra"><span className="artifact-section-label">Metadata</span><div className="artifact-extra-grid">{Object.entries(entry.extra).map(([k, v]) => { if (v === "" || v == null) return null; return (<div key={k} className="artifact-extra-item"><span className="artifact-extra-key">{k.split("_").map(w => w.charAt(0).toUpperCase()+w.slice(1)).join(" ")}</span><span className="artifact-extra-value">{String(v)}</span></div>); })}</div></div>}</div></Collapsible>); }
