import "./ErrorBanner.css";
const HINTS: Record<string, string> = { VALIDATION_ERROR: "Check the field format: table.column", AUTH_ERROR: "Verify credentials in .env", CONNECTION_ERROR: "Make sure your instance is awake", TIMEOUT_ERROR: "Try waking your PDI via browser", INTERNAL_ERROR: "Check API server terminal" };
export function ErrorBanner({ message, code, onDismiss }: { message: string; code?: string | null; onDismiss?: () => void }) { const hint = code ? HINTS[code] : undefined; return (<div className="error-banner"><div className="error-banner-content"><div className="error-banner-icon">✕</div><div className="error-banner-text"><p className="error-banner-message">{message}</p>{hint && <p className="error-banner-hint">💡 {hint}</p>}{code && <span className="error-banner-code">{code}</span>}</div></div>{onDismiss && <button className="error-banner-dismiss" onClick={onDismiss}>Dismiss</button>}</div>); }
