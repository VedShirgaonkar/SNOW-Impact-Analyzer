import { useState, type ReactNode } from "react"; import "./Collapsible.css";
export function Collapsible({ title, badge, defaultOpen = false, children, variant = "default" }: { title: string; badge?: string | number; defaultOpen?: boolean; children: ReactNode; variant?: "default" | "subtle" }) { const [open, setOpen] = useState(defaultOpen); return (<div className={`collapsible collapsible-${variant} ${open ? "open" : ""}`}><button className="collapsible-header" onClick={() => setOpen(!open)}><span className={`collapsible-chevron ${open ? "rotated" : ""}`}>▶</span><span className="collapsible-title">{title}</span>{badge !== undefined && <span className="collapsible-badge">{badge}</span>}</button>{open && <div className="collapsible-body">{children}</div>}</div>); }
